import tensorflow as tf
import datetime
import os
import argparse
import glob

from model.model import Net

class Solver:
    
    def load_data(self, image_dir, label_dir):
        image_path = dir+'/*.jpg'
    
    
    def train(self):
        